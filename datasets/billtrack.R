


if("installr" %in% installed.packages()==FALSE){
  install.packages("airtable")} else {
    installr::install.R()}

if("rairtable" %in% installed.packages()==FALSE){
  install.packages("rairtable")} else {
    library(rairtable)}
